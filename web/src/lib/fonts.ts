// Font definitions for StudentCareer AI
// Uses standard, robust CSS font-family stacks with full cross-platform compatibility

export const inter = {
  className: "font-sans",
  variable: "--font-inter",
};

export const instrumentSerif = {
  className: "font-serif",
  variable: "--font-instrument-serif",
};

export const instrumentSerifItalic = {
  className: "font-serif italic",
  variable: "--font-instrument-serif-italic",
};
